import { Logger } from '@stryker-mutator/api/logging';
import { StrykerOptions } from '@stryker-mutator/api/core';
import { Config } from '@jest/types';
import JestConfigLoader from './jest-config-loader';
import { NodeRequireFunction } from './node-require-function';
/**
 * The Default config loader will load the Jest configuration using the package.json in the package root
 */
export default class CustomJestConfigLoader implements JestConfigLoader {
    private readonly log;
    private readonly options;
    private readonly require;
    private readonly projectRoot;
    static inject: ["logger", "options", "loader", "projectRoot"];
    constructor(log: Logger, options: StrykerOptions, require: NodeRequireFunction, projectRoot: string);
    loadConfig(): Config.InitialOptions;
    private readConfigFromJestConfigFile;
    private readConfigFromPackageJson;
}
//# sourceMappingURL=custom-jest-config-loader.d.ts.map