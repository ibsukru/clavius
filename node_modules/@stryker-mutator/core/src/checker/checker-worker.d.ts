import { Checker, CheckResult } from '@stryker-mutator/api/check';
import { StrykerOptions, Mutant } from '@stryker-mutator/api/core';
import { PluginContext, Injector } from '@stryker-mutator/api/plugin';
export declare class CheckerWorker implements Checker {
    private readonly innerCheckers;
    static inject: ["options", "$injector"];
    constructor(options: StrykerOptions, injector: Injector<PluginContext>);
    init(): Promise<void>;
    check(mutant: Mutant): Promise<CheckResult>;
}
//# sourceMappingURL=checker-worker.d.ts.map