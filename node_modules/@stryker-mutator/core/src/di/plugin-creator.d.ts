import { commonTokens, PluginContext, PluginInterfaces, PluginKind } from '@stryker-mutator/api/plugin';
import { InjectableFunctionWithInject } from 'typed-inject';
export declare class PluginCreator<TPluginKind extends PluginKind> {
    private readonly kind;
    private readonly pluginResolver;
    private readonly injector;
    private constructor();
    create(name: string): PluginInterfaces[TPluginKind];
    private isFactoryPlugin;
    private isClassPlugin;
    static createFactory<TPluginKind extends PluginKind, TContext extends PluginContext>(kind: TPluginKind): InjectableFunctionWithInject<TContext, PluginCreator<TPluginKind>, [typeof commonTokens.pluginResolver, typeof commonTokens.injector]>;
}
//# sourceMappingURL=plugin-creator.d.ts.map