import { PartialStrykerOptions } from '@stryker-mutator/api/core';
import { StrykerOptions } from '@stryker-mutator/api/core';
import { Logger } from '@stryker-mutator/api/logging';
import { OptionsValidator } from './options-validator';
export declare const CONFIG_SYNTAX_HELP: string;
export default class ConfigReader {
    private readonly cliOptions;
    private readonly log;
    private readonly validator;
    static inject: ["cliOptions", "logger", "optionsValidator"];
    constructor(cliOptions: PartialStrykerOptions, log: Logger, validator: OptionsValidator);
    readConfig(): StrykerOptions;
    private loadConfigModule;
    private resolveConfigFile;
}
//# sourceMappingURL=config-reader.d.ts.map