import { StrykerOptions } from '@stryker-mutator/api/core';
import { Logger } from '@stryker-mutator/api/logging';
import type { JSONSchema7 } from 'json-schema';
export declare class OptionsValidator {
    private readonly log;
    private readonly validateFn;
    static readonly inject: ["validationSchema", "logger"];
    constructor(schema: JSONSchema7, log: Logger);
    validate(options: Record<string, unknown>): asserts options is StrykerOptions;
    private removeDeprecatedOptions;
    private additionalValidation;
    private schemaValidate;
    private throwErrorIfNeeded;
}
export declare function defaultOptions(): StrykerOptions;
export declare function validateOptions(options: Record<string, unknown>, optionsValidator: OptionsValidator): StrykerOptions;
export declare namespace validateOptions {
    var inject: ["options", "optionsValidator"];
}
export declare function markUnknownOptions(options: StrykerOptions, schema: JSONSchema7, log: Logger): StrykerOptions;
export declare namespace markUnknownOptions {
    var inject: ["options", "validationSchema", "logger"];
}
//# sourceMappingURL=options-validator.d.ts.map