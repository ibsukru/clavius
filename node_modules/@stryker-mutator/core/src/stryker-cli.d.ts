import * as commander from 'commander';
import { PartialStrykerOptions } from '@stryker-mutator/api/core';
import { MutantResult } from '@stryker-mutator/api/report';
export default class StrykerCli {
    private readonly argv;
    private readonly program;
    private readonly runMutationTest;
    private command;
    private strykerConfig;
    constructor(argv: string[], program?: commander.Command, runMutationTest?: (options: PartialStrykerOptions) => Promise<MutantResult[]>);
    run(): void;
}
//# sourceMappingURL=stryker-cli.d.ts.map