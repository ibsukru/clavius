"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RESOURCES_DIR_NAME = void 0;
const path = require("path");
const plugin_1 = require("@stryker-mutator/api/plugin");
const fileUrl = require("file-url");
const bind_mutation_test_report_1 = require("./templates/bind-mutation-test-report");
const HtmlReporterUtil = require("./html-reporter-util");
const DEFAULT_BASE_FOLDER = path.normalize('reports/mutation/html');
exports.RESOURCES_DIR_NAME = 'strykerResources';
class HtmlReporter {
    constructor(options, log) {
        this.options = options;
        this.log = log;
    }
    onMutationTestReportReady(report) {
        this.mainPromise = this.generateReport(report);
    }
    wrapUp() {
        return this.mainPromise;
    }
    async generateReport(report) {
        const indexFileName = path.resolve(this.baseDir, 'index.html');
        await this.cleanBaseFolder();
        await Promise.all([
            HtmlReporterUtil.copyFile(require.resolve('mutation-testing-elements/dist/mutation-test-elements.js'), path.resolve(this.baseDir, 'mutation-test-elements.js')),
            HtmlReporterUtil.copyFile(path.resolve(__dirname, 'templates', 'stryker-80x80.png'), path.resolve(this.baseDir, 'stryker-80x80.png')),
            HtmlReporterUtil.copyFile(path.resolve(__dirname, 'templates', 'index.html'), path.resolve(this.baseDir, 'index.html')),
            HtmlReporterUtil.writeFile(path.resolve(this.baseDir, 'bind-mutation-test-report.js'), bind_mutation_test_report_1.bindMutationTestReport(report)),
        ]);
        this.log.info(`Your report can be found at: ${fileUrl(indexFileName)}`);
    }
    get baseDir() {
        if (!this._baseDir) {
            if (this.options.htmlReporter && this.options.htmlReporter.baseDir) {
                this._baseDir = this.options.htmlReporter.baseDir;
                this.log.debug(`Using configured output folder ${this._baseDir}`);
            }
            else {
                this.log.debug(`No base folder configuration found (using configuration: htmlReporter: { baseDir: 'output/folder' }), using default ${DEFAULT_BASE_FOLDER}`);
                this._baseDir = DEFAULT_BASE_FOLDER;
            }
        }
        return this._baseDir;
    }
    async cleanBaseFolder() {
        await HtmlReporterUtil.deleteDir(this.baseDir);
        await HtmlReporterUtil.mkdir(this.baseDir);
    }
}
exports.default = HtmlReporter;
HtmlReporter.inject = plugin_1.tokens(plugin_1.commonTokens.options, plugin_1.commonTokens.logger);
//# sourceMappingURL=html-reporter.js.map