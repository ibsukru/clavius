import { StrykerOptions } from '@stryker-mutator/api/core';
import { Logger } from '@stryker-mutator/api/logging';
import { MutantResult, mutationTestReportSchema, Reporter } from '@stryker-mutator/api/report';
export default class ClearTextReporter implements Reporter {
    private readonly log;
    private readonly options;
    static inject: ["logger", "options"];
    constructor(log: Logger, options: StrykerOptions);
    private readonly out;
    private writeLine;
    private configConsoleColor;
    onAllMutantsTested(mutantResults: MutantResult[]): void;
    private logMutantResult;
    private colorSourceFileAndLocation;
    private logExecutedTests;
    onMutationTestReportReady(report: mutationTestReportSchema.MutationTestResult): void;
}
//# sourceMappingURL=clear-text-reporter.d.ts.map