import { MutantResult } from 'mutation-testing-report-schema';
/**
 * Class to keep track of the states of the
 * mutants that are active at the cursor while walking the code.
 */
export declare class BackgroundColorCalculator {
    private killed;
    private noCoverage;
    private survived;
    private timeout;
    private ignored;
    readonly markMutantStart: (mutant: Pick<MutantResult, 'status'>) => void;
    readonly markMutantEnd: (mutant: Pick<MutantResult, 'status'>) => void;
    private countMutant;
    determineBackground: () => string | null;
}
//# sourceMappingURL=BackgroundColorCalculator.d.ts.map